import Foundation

// SET UP YOUR RESTAURANT

// TODO: Change this to your restaurant's name
let restaurantName = "My Restaurant"

// TODO: Change this to a welcome message for your restaurant
let welcomeMessage = "Welcome! Check out our menu."

// TODO: Add ONLY 3 menu items (each is a String in quotes, separated by commas)
let menuItems = [
    " ? ",
    
]

// These are the ingredient lists for each menu item.
// DO NOT alter ingredient list!
let ingredients = [
    "flour, gluten, butter",
    "peanut, garlic, olive oil",
    "rice, tomato, basil"
]

//QI: Why do you think you should ONLY add 3 menu items?

// PRINT START OF MENU

print(" == \(restaurantName) == ")
print(welcomeMessage)
print("") // QII: How does this line of code change our output?
