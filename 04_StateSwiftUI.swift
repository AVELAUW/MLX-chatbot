// ────────────────────────────────────────────────────────────
//  SECTION 4: @State  (SwiftUI only — paste into a SwiftUI View)
//  @State stores a value that, when changed, automatically
//  refreshes what the user sees on screen.
// ────────────────────────────────────────────────────────────

//  NOTE: @State only works inside a SwiftUI View struct.
//  The example below is for reference — you will use it in Xcode!

/*
 @State private var showRobot = true   // stores whether the robot is visible

 Button("Hide Robot") {
     showRobot.toggle()                // flips true → false → true …
 }
*/

// TODO: Write a @State variable that stores whether dark mode is ON or OFF
//       e.g., @State private var isDarkMode = false

// TODO: Write a comment describing what would happen on screen
//       if isDarkMode were toggled by a button
